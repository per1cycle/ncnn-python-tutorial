# ncnn-python-tutorial

## How to use this project?

```bash
git submodule update --init
cd ncnn
pip install -e .
cd ..
python examples/hello.py

```

also recommend install torch, for install dataset.

``` bash
pip3 install torch torchvision torchaudio
```